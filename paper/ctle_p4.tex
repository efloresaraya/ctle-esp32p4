\documentclass[lettersize,journal]{IEEEtran}
\usepackage{amsmath,amsfonts}
\usepackage{algorithmic}
\usepackage{algorithm}
\usepackage{array}
\usepackage[caption=false,font=normalsize,labelfont=sf,textfont=sf]{subfig}
\usepackage{tikz}
\usetikzlibrary{patterns, positioning, calc, arrows.meta}
\usepackage{amssymb}
\usetikzlibrary{shapes, arrows.meta, positioning, shadows, calc}
\usepackage{textcomp}
\usepackage{stfloats}
\usepackage{url}
\usepackage{verbatim}
\usepackage{graphicx}
\usepackage{cite}
\usepackage{booktabs}
\hyphenation{op-tical net-works semi-conduc-tor IEEE-Xplore}

\begin{document}

\title{CTLE: A Compressed Tensor-LUT Engine for On-Device TinyLM Inference
       on IoT Microcontrollers with Metaheuristic Codebook Optimization}

\author{Emilio Flores,~\IEEEmembership{Graduate Student Member,~IEEE}%
\thanks{E. Flores is with the Escuela de Ingeniería Informática,
        Universidad de Valparaíso, Valparaíso, Chile,
        and with the Faculty of Engineering and Business,
        Universidad de las Américas, Santiago, Chile
        (e-mail: emilio.flores@postgrado.uv.cl).
        This work was supported by ANID BECAS/DOCTORADO NACIONAL 21242003.}%
}

\markboth{IEEE Internet of Things Journal}%
{Flores: CTLE — Compressed Tensor-LUT Engine for IoT On-Device Inference}

\maketitle

% ─────────────────────────────────────────────────────────────────────────────
\begin{abstract}
Deploying Tiny Language Models (TinyLMs) on low-power IoT microcontrollers is
challenging because model weights exceed on-chip memory and conventional
dequantization requires materializing dense tensors before inference.
This paper presents the Compressed Tensor--LUT Engine (CTLE), a compressed-domain
execution engine for transformer inference on microcontrollers.
CTLE stores each large weight matrix as a learned 16-entry codebook plus packed
4-bit indices, and fuses nibble decoding, LUT access, and accumulation inside
the matrix--vector kernel without reconstructing full-precision weights in RAM.
To improve 4-bit quality, we compare K-means with two warm-started
metaheuristic codebook optimizers, a Genetic Algorithm (GA) and Particle Swarm
Optimization (PSO), both accelerated with Numba JIT.

On a 15M-parameter TinyStories transformer, CTLE-PSO compresses the counted
weights from 60.77\,MB to 7.61\,MB (7.98$\times$) and reduces TinyStories PPL
from 19.23 (K-means) to 15.93 at identical storage.
The gain is concentrated on the 9.2M-entry token embedding matrix, while
K-means is already near-optimal for the smaller projection matrices.
We implement a multi-format C firmware engine on ESP32-P4 Nano
(RISC-V HP@360\,MHz, 32\,MB PSRAM) and compare CTLE against INT4 uniform and
INT4 block-wise baselines.
CTLE achieves 0.965\,tok/s with 21.5\,MB PSRAM free; INT4 block-wise improves
quality but is 31\,\% slower due to per-group scale reads from PSRAM.
Finally, we introduce P-CTLE, a throughput-oriented variant that quantizes
activations to 4 bits and replaces the inner-loop multiply with a product-LUT
lookup. P-CTLE reaches 1.201\,tok/s at the same 7.61\,MB footprint, with the
expected quality trade-off (TinyStories PPL\,=\,33.14), motivating future
CTLE-NPU implementations for FPGA/RISC-V platforms.
\end{abstract}

\begin{IEEEkeywords}
TinyML, Edge AI, Tiny Language Models, Model compression, Post-training
quantization, Codebook quantization, Genetic Algorithm, Particle Swarm
Optimization, Microcontrollers, ESP32-P4, On-device inference.
\end{IEEEkeywords}


% ─────────────────────────────────────────────────────────────────────────────
\section{Introduction}
\label{sec:intro}

Recent progress in large language models (LLMs) has substantially improved
natural language generation and understanding, but at the cost of rapid growth
in parameter count and compute---conflicting with the memory, bandwidth, and
energy budgets of low-power Internet-of-Things (IoT) nodes.
Typical microcontrollers used in IoT endpoints, such as the ESP32-P4, provide
limited on-chip SRAM, external QSPI Flash, and external PSRAM, but offer
neither high-throughput floating-point compute nor the memory bandwidth needed
to repeatedly materialize large weight tensors.
Deploying non-trivial language models on such devices requires aggressive
compression, or offloading inference to the cloud---which reduces autonomy and
raises privacy and availability concerns.

TinyML research addresses microcontroller constraints through model compression,
post-training quantization, efficient kernels, and MCU-oriented inference
frameworks~\cite{Saha2022,Novac2021,Elhanashi2024}.
Compact recurrent and sequence models demonstrate that small networks can be
tailored to strict resource envelopes~\cite{Kusupati2018,Kannan2021,Cheng2024}.
In parallel, GPU-targeted 4-bit quantization methods such as
GPTQ~\cite{frantar2023gptq} and SqueezeLLM~\cite{kim2024squeezellm} achieve
near-lossless compression on large LLMs, but require GPU VRAM for inference
and do not target streaming execution on Flash-only microcontrollers.

This work introduces the Compressed Tensor--LUT Engine (CTLE), an inference
engine that executes transformer layers directly from a 4-bit weight-sharing
representation on ESP32-P4-class microcontrollers.
Each weight matrix is represented by a tensor-wise codebook of $K=16$
real-valued centroids and a matrix of 4-bit indices, packed as two indices per
byte and stored in QSPI Flash.
Rather than dequantizing weights into SRAM, CTLE performs a fused
Tensor--LUT computation in which indices are decoded on-the-fly inside a
streaming matrix--vector kernel.
CTLE is therefore not merely a storage format: it is a compressed-domain
execution engine that fuses nibble decoding, LUT access, and accumulation
without materializing dequantized tensors.

A key contribution of this paper is the application of \emph{metaheuristic
codebook optimization}. Standard K-means converges to local minima that
may be suboptimal for large, non-Gaussian weight distributions.
We propose GA and PSO variants with warm start from K-means, Numba JIT
acceleration, and stochastic fitness subsampling, enabling high-quality
codebook search within minutes on a laptop CPU.
PSO, in particular, improves over K-means by $-$14.9\,\% relative MSE on
the critical embedding tensor (9.2\,M weights), translating to
$-$17.2\,\% PPL reduction at zero additional memory cost.
This behaviour is important for embedded deployment: metaheuristic search is
most useful on large, high-impact tensors, whereas K-means is already
near-optimal on small projection matrices.

We instantiate CTLE on a 15M-parameter TinyStories language model and
demonstrate end-to-end on-device inference on an ESP32-P4 Nano, achieving
0.965\,tok/s. We compare against INT4 uniform and INT4 block-wise baselines
(our own implementations) and GPU-targeting methods from the literature.

\paragraph{Main contributions.}
\begin{itemize}
    \item CTLE, a firmware-level inference engine for ESP32-P4-class
    microcontrollers that streams packed 4-bit weight-sharing tensors from
    external QSPI Flash and computes transformer matvecs without materializing
    dense dequantized weight buffers in SRAM/PSRAM.

    \item A fused Tensor--LUT matrix--vector kernel that decodes packed 4-bit
    indices on-the-fly and performs lookup-and-accumulate, reducing transient
    weight-expansion memory from $\mathcal{O}(MN)$ floating-point storage to
    $\mathcal{O}(1)$ additional working state, while retaining a 16-entry LUT
    per tensor.

    \item \textbf{Metaheuristic codebook optimization}: GA (BLX-$\alpha$
    crossover, tournament selection, elitism) and PSO (inertia-weight decay
    $\omega{:}\ 0.9\!\to\!0.4$, warm start from K-means) with Numba JIT
    acceleration---both achieving compression in under 4 minutes on a laptop.

    \item A full comparative evaluation including INT4 Uniform, INT4 Block-wise
    ($G{=}32$), CTLE+K-means, CTLE+GA, and CTLE+PSO, with hardware
    benchmark on ESP32-P4 Nano at 0.965\,tok/s.

    \item A multi-format C firmware engine supporting TAG\_F32,
    TAG\_CTLE, TAG\_INT4U, TAG\_INT4BW, and TAG\_PCTLE, enabling fair
    on-device comparison across all schemes and a P-CTLE product-LUT variant
    for throughput-oriented or FPGA/NPU-oriented execution.
\end{itemize}


% ─────────────────────────────────────────────────────────────────────────────
\section{Related Work}
\label{sec:related_work}

\subsection{TinyML and MCU deployment}
Saha et al.\ review ML on microcontroller hardware, emphasizing that SRAM
capacity, energy, and bandwidth are primary bottlenecks~\cite{Saha2022}.
Novac et al.\ present MicroAI for training, quantizing, and deploying networks
on low-power MCUs~\cite{Novac2021}.
Elhanashi et al.\ survey TinyML advances for IoT devices, focusing on
compression methods and accelerators~\cite{Elhanashi2024}.
Sakr proposes LUT-based binarized networks for tiny devices~\cite{Sakr2023}.
Cerioli et al.\ propose a TinyML compiler that targets C with LUT-based
activations~\cite{Cerioli2025}.

\subsection{Compressed and efficient sequence models}
Kusupati et al.\ introduce FastGRNN for MCU-class
inference~\cite{Kusupati2018}.
Kannan and Hoffmann propose Budget RNNs for multiple energy
budgets~\cite{Kannan2021}.
Cheng et al.\ present dynamic gated recurrent networks to reduce
computation~\cite{Cheng2024}.

\subsection{Hardware accelerators}
Lee et al.\ propose an FPGA-based RNN accelerator~\cite{Lee2021}.
Chen et al.\ present Eciton, a low-power FPGA accelerator for edge
nodes~\cite{Chen2024}.
Muzamal et al.\ propose a reconfigurable MLP--RNN processor with LUT-based
multipliers~\cite{Muzamal2021}.
Lin et al.\ introduce LUTIN, converting matvecs to table lookups for low-bit
inference~\cite{Lin2024}.

\subsection{LUT-based runtime optimization}
Rezk et al.\ review LUT-based nonlinearities in RNN
deployments~\cite{Rezk2020}.
Rusci et al.\ propose mixed FP16--INT8 quantization with LUT-backed activations
for MCU speech enhancement~\cite{Rusci2022}.
Alvanaki investigates DVFS and data layout for energy-efficient TinyML on
STM32 MCUs~\cite{Alvanaki2024}.

\subsection{GPU-targeted LLM quantization}
GPTQ~\cite{frantar2023gptq} applies one-shot weight quantization using
approximate second-order information, achieving 4-bit compression of large
LLMs (e.g., OPT-125M, WikiText-2 PPL\,=\,31.12) on GPU.
SqueezeLLM~\cite{kim2024squeezellm} combines dense-and-sparse quantization with
non-uniform codebooks, attaining PPL\,=\,5.79 at 3.45 bits for LLaMA-7B on GPU.
LLM.int8()~\cite{dettmers2022llmint8} uses mixed-precision decomposition for
8-bit matrix multiplication at scale.
AWQ~\cite{lin2024awq} applies activation-aware weight quantization to 4-bit,
preserving channels with high activation magnitude.
These methods target GPU inference and either require VRAM or dense
dequantization; they do not provide streaming-from-Flash MCU execution.
Accordingly, our comparison with these methods is about deployment target and
runtime assumptions, not absolute PPL across different model families.

\subsection{Gap with respect to CTLE}
No prior work jointly provides:
(a) a tensor-wise 4-bit codebook-plus-index representation
\emph{learned via metaheuristics} (GA/PSO) from each tensor's weight
distribution;
(b) direct streaming execution on MCU-class hardware in C without intermediate
weight-expansion buffers; and
(c) a multi-format firmware engine that enables fair on-device comparison
between LUT-codebook and standard INT4 baselines on the same RISC-V MCU.


% ─────────────────────────────────────────────────────────────────────────────
% ── Figure 1: CTLE architecture ─────────────────────────────────────────────
\definecolor{techBlue}{RGB}{230, 240, 255}
\definecolor{techOrange}{RGB}{255, 245, 220}
\definecolor{techGreen}{RGB}{220, 250, 230}
\definecolor{lineGray}{RGB}{80, 80, 80}

\begin{figure*}[t]
    \centering
    \begin{tikzpicture}[
        node distance=1.5cm and 2cm,
        font=\footnotesize,
        box/.style={draw, thick, rounded corners=4pt, align=center,
            minimum width=3.5cm, minimum height=1.6cm, inner sep=4pt,
            drop shadow={opacity=0.25,shadow xshift=2pt,shadow yshift=-2pt}},
        data/.style={box, fill=techBlue, draw=techBlue!60!black},
        memory/.style={box, fill=techOrange, draw=techOrange!60!black},
        compute/.style={box, fill=techGreen, draw=techGreen!60!black},
        link/.style={->, >=Stealth, ultra thick, color=lineGray,
            rounded corners=5pt},
        labeltext/.style={font=\scriptsize\itshape, text=black, align=center,
            fill=white, inner sep=1.5pt, fill opacity=0.9}
    ]
    \node[compute] (engine) at (0,0) {
        \textbf{CTLE}\\
        \scriptsize On-the-fly lookup\\
        \scriptsize $\textrm{acc} {+}= \mathcal{L}[\textrm{idx}] \cdot x$
    };
    \node[data] (x) at (-5,0) {
        \textbf{Input Activation}\\
        $\mathbf{x} \in \mathbb{R}^{d}$
    };
    \node[data] (y) at (5,0) {
        \textbf{Output Activation}\\
        $\mathbf{y} = W\mathbf{x}$
    };
    \node[memory] (weights) at (0,3) {
        \textbf{Compressed Weights (Flash)}\\
        \scriptsize LUT $\mathcal{L} \in \mathbb{R}^{16}$ + 4-bit indices
    };
    \draw[link] (x.east) --
        node[labeltext,midway,above=2pt]{stream of activations}(engine.west);
    \draw[link] (weights.south) --
        node[labeltext,midway,right=1pt]{stream indices} (engine.north);
    \draw[link] (engine.east) --
        node[labeltext,midway,above=2pt]{accumulated results}(y.west);
    \node[above=0.1cm of weights, color=techOrange!60!black,
        font=\scriptsize\bfseries]{Flash/PSRAM — no reconstruction buffer};
    \node[below=0.1cm of engine, color=techGreen!60!black,
        font=\scriptsize\bfseries]{LUT = 64 B local working set};
    \end{tikzpicture}
    \caption{CTLE architecture. Packed 4-bit indices stream from Flash; the
    64-byte LUT is small enough to remain register- or cache-resident during
    the hot loop. No dense weight matrix is ever materialized in SRAM/PSRAM.}
    \label{fig:ctle_arch}
\end{figure*}


% ─────────────────────────────────────────────────────────────────────────────
% ── Figure 2: Parameter representation comparison ───────────────────────────
\begin{figure*}[t]
\centering
\begin{tikzpicture}[
    font=\footnotesize,
    box/.style={draw, rounded corners=3pt, align=center,
                minimum width=4.5cm, minimum height=1.4cm, inner sep=4pt},
    dense/.style={box, fill=white},
    int8/.style={box, fill=gray!12},
    ctle/.style={box, fill=techGreen!35},
]
% ── header ──
\node[font=\small\bfseries] at (0,2.0)
    {Parameter representation and transient RAM per weight matrix $W$};
% ── three format rows ──
\node[dense] (fp32) at (0, 0.8) {
    \textbf{Dense FP32}\\
    \scriptsize Storage: $32$\,bits/weight\quad
    Transient RAM: full $W\in\mathbb{R}^{M\times N}$ (FP32)
};
\node[int8]  (int8) at (0,-0.5) {
    \textbf{Dense INT4/INT8}\\
    \scriptsize Storage: $4$--$8$\,bits/weight $+$ scale(s)\quad
    Transient RAM: dequantized $\tilde{W}$ $+$ scale table
};
\node[ctle]  (ctlebox) at (0,-1.8) {
    \textbf{CTLE 4-bit LUT (ours)}\\
    \scriptsize Storage: LUT (16 floats, 64\,B) $+$ 4\,bits/weight\quad
    Transient RAM: none — matvec runs in compressed space
};
% ── right-side annotations ──
\draw[->, thick] (fp32.east)    -- +(2.0,0)
    node[right, align=left, font=\scriptsize] {baseline quality\\full RAM footprint};
\draw[->, thick] (int8.east)    -- +(2.0,0)
    node[right, align=left, font=\scriptsize] {$4\text{--}8\times$ smaller\\dequant buffer required};
\draw[->, thick] (ctlebox.east) -- +(2.0,0)
    node[right, align=left, font=\scriptsize] {$8\times$ smaller\\no dense $W$ in RAM};
\end{tikzpicture}
\caption{Parameter representation comparison. Dense FP32 and standard INT4/INT8 require
    a full dequantized weight matrix in transient SRAM before every matvec.
    CTLE replaces this with a 64-byte codebook and streams 4-bit indices directly into
    the accumulate loop, eliminating the dequantization buffer entirely.}
\label{fig:representation}
\end{figure*}

% ─────────────────────────────────────────────────────────────────────────────
\section{Methodology}
\label{sec:methodology}

\subsection{CTLE representation: codebook and 4-bit indices}
\label{subsec:representation}

Consider a weight matrix $\mathbf{W}\in\mathbb{R}^{M\times N}$.
CTLE replaces $\mathbf{W}$ by:
(i) a codebook $\mathcal{L}\in\mathbb{R}^{K}$, $K=16$, and
(ii) an index matrix $\mathbf{I}\in\{0,\dots,K-1\}^{M\times N}$.
The quantizer is
\begin{equation}
Q(w,\mathcal{L}) \triangleq
\mathcal{L}\!\left[\arg\min_{k} |w - \mathcal{L}[k]|\right].
\label{eq:quantizer}
\end{equation}
Codebook learning minimizes mean squared reconstruction error:
\begin{equation}
\mathcal{L}^{*}=\arg\min_{\mathcal{L}\in\mathbb{R}^{K}}
\frac{1}{MN}\sum_{m,n}\Bigl(\mathbf{W}_{m,n}-Q(\mathbf{W}_{m,n},\mathcal{L})\Bigr)^2.
\label{eq:objective}
\end{equation}
We apply CTLE to all large tensors (token embedding and $W_q,W_k,W_v,W_o,
W_1,W_2,W_3$ per layer); small tensors (RMSNorm gains) are kept in FP32.

\subsection{K-means codebook learning}
\label{subsec:kmeans}

Equation~\eqref{eq:objective} is solved by 1D K-means on the flattened weight
values ($N_\mathrm{iter}=300$, percentile initialization across $K$ quantiles).
This is fast ($<$2\,s per tensor) and provides the warm-start codebook for GA
and PSO.

\subsection{Genetic Algorithm codebook optimization (CTLE+GA)}
\label{subsec:ga}

We apply a steady-state GA to refine the K-means codebook.
Each individual is a codebook vector $\mathcal{L}\in\mathbb{R}^K$ with fitness
equal to negative reconstruction MSE on a random subsample of at most
50{,}000 weights (stochastic fitness evaluation).

\paragraph{Operators.}
\textit{Selection}: binary tournament ($k=3$).
\textit{Crossover}: BLX-$\alpha$ ($\alpha=0.3$, $p_\mathrm{cross}=0.8$):
for each coordinate $j$, $\ell_j^{\mathrm{child}} = u$ where $u$ is sampled
uniformly from
$[\min(\ell_j^{p_1},\ell_j^{p_2})-\alpha\delta_j,\,
   \max(\ell_j^{p_1},\ell_j^{p_2})+\alpha\delta_j]$,
$\delta_j=|\ell_j^{p_1}-\ell_j^{p_2}|$.
\textit{Mutation}: Gaussian perturbation of random codebook entries
($p_\mathrm{mut}=0.15$, $\sigma=0.05\,\sigma_\mathbf{W}$).
\textit{Elitism}: top-1 individual carried into next generation.

\paragraph{Numba acceleration.}
The hot fitness loop (\textit{assign} + \textit{MSE}) is compiled with
\texttt{@njit(cache=True)}, reducing runtime from $\sim$hours to $\sim$208\,s
for the full 15M-parameter model on a laptop CPU.

\subsection{Particle Swarm Optimization codebook (CTLE+PSO)}
\label{subsec:pso}

PSO maintains a swarm of $N_p=60$ particles, each encoding a codebook
$\mathcal{L}\in\mathbb{R}^K$. Particles are initialized from K-means warm start
with random perturbation ($\sigma=0.1\,\sigma_\mathbf{W}$).

\paragraph{Update rule.}
At iteration $t$, each particle $i$ updates velocity $\mathbf{v}_i$ and
position $\mathbf{p}_i$ as
\begin{align}
\mathbf{v}_i^{t+1} &= \omega_t \mathbf{v}_i^{t}
  + c_1 r_1 (\mathbf{p}_i^\mathrm{best} - \mathbf{p}_i^{t})
  + c_2 r_2 (\mathbf{g}^\mathrm{best} - \mathbf{p}_i^{t}), \\
\mathbf{p}_i^{t+1} &= \mathbf{p}_i^{t} + \mathbf{v}_i^{t+1},
\end{align}
with $c_1 = c_2 = 2.0$, $r_1,r_2 \sim \mathcal{U}(0,1)$, and
linearly decaying inertia $\omega_t = \omega_\mathrm{max} -
(\omega_\mathrm{max}-\omega_\mathrm{min})\,t/(T-1)$,
$\omega_\mathrm{max}=0.9$, $\omega_\mathrm{min}=0.4$.
Velocities are clipped to $\pm v_\mathrm{max}=0.1\,(\max\mathbf{W}-\min\mathbf{W})$.
Fitness is evaluated on a 50{,}000-weight random subsample.
The hot kernel is compiled with Numba JIT, achieving $\sim$198\,s total for the
full model.

\subsection{4-bit nibble packing and Flash layout}
\label{subsec:packing}

Two consecutive indices $\eta_0,\eta_1\in\{0,\dots,15\}$ are packed into one
byte:
\begin{equation}
\beta = \eta_0 \;\lor\; (\eta_1 \ll 4),
\end{equation}
requiring $\lceil MN/2\rceil$ bytes per matrix.
Each CTLE block is serialized as
$\bigl[\mathcal{L}\ \Vert\ \mathbf{I}_\mathrm{pack}\bigr]$
(16 FP32 + packed bytes), concatenated in a fixed tensor order with a 40-byte
header (magic, version, model dimensions, flags).

\subsection{CTLE inference engine}
\label{subsec:engine}

The matrix--vector product is computed without expanding $\mathbf{W}$:
\begin{equation}
y_m = \sum_{n=0}^{N-1} \mathcal{L}\!\left[I_{m,n}\right] x_n,
\label{eq:matvec}
\end{equation}
streaming packed bytes and decoding two indices per byte in the hot loop
(Algorithm~\ref{alg:ctle}). The 16-entry LUT (64\,B) is small enough to remain
register- or cache-resident during the row loop.

\begin{algorithm}[t]
\caption{CTLE fused Tensor--LUT kernel ($N$ even).}
\label{alg:ctle}
\begin{algorithmic}[1]
\REQUIRE $\mathbf{x}\in\mathbb{R}^N$, packed $\mathbf{P}$, codebook
$\mathcal{L}\in\mathbb{R}^{16}$
\ENSURE $\mathbf{y}\in\mathbb{R}^M$
\FOR{$m = 0$ \textbf{to} $M-1$}
    \STATE $\mathrm{acc} \gets 0$
    \FOR{$j = 0$ \textbf{to} $N/2-1$}
        \STATE $B \gets \mathbf{P}[m,j]$
        \STATE $\mathrm{acc} \gets \mathrm{acc}
               + x_{2j}\cdot\mathcal{L}[B\,\&\,\mathtt{0x0F}]
               + x_{2j+1}\cdot\mathcal{L}[B\!\gg\!4]$
    \ENDFOR
    \STATE $y_m \gets \mathrm{acc}$
\ENDFOR
\end{algorithmic}
\end{algorithm}

\subsection{INT4 baselines (our implementation)}
\label{subsec:int4baselines}

For a fair comparison we implement two standard INT4 baselines in the same
Python pipeline and C firmware.

\paragraph{INT4 Uniform (TAG\_INT4U).}
One symmetric scale per tensor:
$s = \max|\mathbf{W}|/7 + \epsilon$,
$q_{m,n} = \mathrm{clip}(\mathrm{round}(w_{m,n}/s), -7, 7)$,
stored as unsigned nibble $q+7\in[0,14]$.
Storage: $4 + \lceil MN/2\rceil$ bytes.

\paragraph{INT4 Block-wise (TAG\_INT4BW).}
One symmetric scale per row-group of $G=32$ columns:
$s_{m,g} = \max_{c\in\mathcal{G}(m,g)}|w_{m,c}|/7 + \epsilon$.
Storage: $4MN_g + \lceil MN/2\rceil$ bytes,
$N_g=\lceil N/G\rceil$ (effective $\approx5$\,bits/weight).

\paragraph{Multi-format C firmware.}
\texttt{WeightBlock} stores tag, nibble pointer, and---depending on format---a
16-entry LUT (CTLE), a scalar scale (INT4U), or a scale array
\texttt{scales[rows$\times$n\_groups]} (INT4BW).
Dispatch in \texttt{matvec()} selects the appropriate kernel at runtime.

\subsection{Autoregressive execution on ESP32-P4 Nano}
\label{subsec:hw}

The firmware implements a Llama-2 decoder: RMSNorm, CTLE matvec for all
projections ($W_q,W_k,W_v,W_o,W_1,W_2,W_3$), RoPE (precomputed
$\sin/\cos$ tables), causal KV-cache in PSRAM, SwiGLU FFN, and
tied-weight output projection.
The model is stored on SPIFFS (partition at 0x310000), loaded into PSRAM at
boot ($\sim$2.7\,s), and benchmarked with three hardcoded prompts.
Sampling uses temperature\,=\,0.8 and top-$p$\,=\,0.9.


% ─────────────────────────────────────────────────────────────────────────────
\section{Comparative Analysis of Quantization Schemes}
\label{sec:analysis}

We compare four 4-bit representations for computing $\mathbf{y}=\mathbf{W}\mathbf{x}$
on MCU-class hardware.

\paragraph{A. INT4 Uniform (tensor-wise scale).}
$y_m = s\sum_n q_{m,n}x_n$ with a single per-tensor scale $s$.
Simple and fast but severely limited by the non-uniform weight distribution of
large embedding matrices.

\paragraph{B. INT4 Block-wise (group-wise scale).}
$y_m = \sum_g s_{m,g}\sum_{n\in\mathcal{G}(m,g)}q_{m,n}x_n$.
Better fidelity at the cost of per-group scale reads and FP32 conversion in the
hot loop.
Scale storage: $M\lceil N/G\rceil\!\cdot\!4$ bytes (FP32 in our implementation).

\paragraph{C. K-means codebook with expansion (expand-then-compute).}
Reconstruct dense $\tilde{\mathbf{W}}\in\mathbb{R}^{M\times N}$ then multiply.
Requires $4MN$ bytes of transient SRAM---infeasible for large tensors on MCUs.

\paragraph{D. CTLE fused Tensor--LUT (proposed).}
Decodes indices on-the-fly (Eq.~\ref{eq:matvec}).
Transient memory: $\mathcal{O}(1)$ (accumulator + 64-byte LUT in registers).
Compressed storage: $\lceil MN/2\rceil + 64$ bytes.
Eliminates scale tables, dequantization paths, and weight-expansion buffers.


% ─────────────────────────────────────────────────────────────────────────────
\section{Experimental Results}
\label{sec:results}

\subsection{Setup}
\label{sec:setup}

\paragraph{Model.}
TinyStories-15M (Llama-2 architecture,
$d\!=\!288$, $n_\mathrm{layers}\!=\!6$, $n_\mathrm{heads}\!=\!6$,
$d_\mathrm{FFN}\!=\!768$, $\mathrm{vocab}\!=\!32\,000$);
weights from the karpathy/tinyllamas HuggingFace
repository~\cite{karpathy2023llama2c}.

\paragraph{Evaluation corpora.}
\textit{TinyStories (TS):} 10 in-distribution sentences (192 tokens scored).
\textit{WikiText-2 (WT2):} standard benchmark test split~\cite{merity2016pointer},
325,693 tokens scored via non-overlapping sliding windows of 256 tokens
(stride\,=\,256), using the LLaMA SentencePiece tokenizer.
NLL is mean cross-entropy per token; $\mathrm{PPL}=\exp(\mathrm{NLL})$.
WT2 absolute PPL values reflect out-of-distribution evaluation
(TinyStories-15M was not trained on Wikipedia) and are not directly comparable
to published numbers for general-purpose LLMs; the WT2 column reports
the \emph{relative} ordering across compression methods on a standard corpus.
In particular, the FP32 TinyStories-15M baseline already has high WT2 PPL
(6{,}127); CTLE's WT2 values should therefore be read as degradation relative
to this small, domain-specific base model, not as a cross-paper quality ranking.

\paragraph{Compression pipeline.}
Script \texttt{compress.py}, argument \texttt{--method} $\in$
\texttt{\{kmeans, ga, pso, int4\_uniform, int4\_blockwise, pctle\}};
all runs use \texttt{--k~16}, seed\,=\,42.
Compression timing was measured on an Apple M-series laptop (no GPU).

\paragraph{Hardware target.}
ESP32-P4 Nano: RISC-V HP core @360\,MHz (silicon rev\,v1.3 hardware cap;
SDK configured for 400\,MHz), 32\,MB QSPI PSRAM, 16\,MB Flash, ESP-IDF v6.1.

\subsection{Quantization ablation}
\label{sec:ablation}

Table~\ref{tab:ablation} compares all six compression schemes on both
evaluation corpora.
INT4 Uniform collapses catastrophically (PPL\,=\,11{,}070 on TS;
533{,}069 on WT2): a single global scale is unable to represent the dynamic
range of the 9.2M-entry embedding matrix (32{,}000$\times$288 weights),
which spans multiple orders of magnitude.
INT4 Block-wise ($G\!=\!32$) recovers quality (PPL\,=\,10.71 / 7{,}120 on
TS/WT2) but increases footprint by 25\,\% (9.51\,MB vs.\ 7.61\,MB) and
requires loading and converting a per-group scale table during inference.

CTLE+K-means achieves PPL\,=\,19.23 / 19{,}239 (TS/WT2) at 7.61\,MB
(7.98$\times$) with compression completed in 74\,s.
CTLE+GA marginally improves to PPL\,=\,18.98 / 19{,}114 in 208\,s.
CTLE+PSO achieves the best CTLE result (PPL\,=\,15.93 / 16{,}827,
$-$17.2\,\% / $-$12.5\,\% vs.\ K-means on TS/WT2) in 198\,s, driven
entirely by better optimization of the embedding tensor
(Table~\ref{tab:relmse}).
The consistent ranking across both corpora---an in-distribution TinyStories
corpus and the out-of-distribution WikiText-2 benchmark---confirms that the
PSO improvement is not a corpus-specific artefact.

\input{../tables/table_ablation}

\subsection{Per-tensor reconstruction error}
\label{sec:relmse}

Table~\ref{tab:relmse} shows relative MSE per tensor.
For small projection matrices ($\leq$288$\times$288\,=\,82\,K weights),
K-means already finds near-optimal solutions and GA/PSO provide no improvement
(identical mean RelMSE across all three methods: 0.01264--0.01265).
PSO's gain concentrates exclusively on \texttt{tok\_emb}
($-$14.9\,\% RelMSE: 0.01190$\to$0.01013), the largest tensor by two
orders of magnitude.
This demonstrates that metaheuristic exploration pays off specifically for
large, high-impact tensors where K-means' greedy updates are prone to local
minima, rather than uniformly improving every matrix.

\input{../tables/table_relmse}

\subsection{Comparison with published methods}
\label{sec:sota}

Table~\ref{tab:sota} contrasts CTLE with GPU-targeting quantization methods
from a deployment perspective.
GPTQ, SqueezeLLM, and LLM.int8() achieve excellent quality on large models
but require GPU VRAM for inference and cannot stream weights from
MCU-class Flash.
CTLE+PSO achieves 7.98$\times$ compression with streaming Flash execution and
zero weight reconstruction in SRAM, at the cost of training on a
smaller model (TinyStories-15M vs.\ billion-parameter models).
Thus, Table~\ref{tab:sota} should not be interpreted as an absolute perplexity
leaderboard; it separates GPU-resident LLM compression from MCU-resident,
Flash-streaming TinyLM execution.

\input{../tables/table_sota}

\subsection{Hardware benchmark on ESP32-P4 Nano}
\label{sec:hw_bench}

Table~\ref{tab:hw} reports on-device benchmarks for all four deployed compression
formats across three fixed prompts (4, 8, and 6 prompt tokens, 64 tokens
generated each).
The same pre-tokenised sequences and RNG seed (0xCAFEBABE) are used in
every run; generation parameters are temperature\,$=0.8$, top-$p=0.9$.

\input{../tables/table_hw}

\paragraph{Throughput.}
CTLE achieves $\mathbf{1{,}035.9\pm1.1}$\,ms/tok ($\mathbf{0.965\pm0.001}$\,tok/s,
mean\,$\pm$\,$\sigma$ across three prompts), equal to INT4U within measurement
noise ($1{,}040.6\pm0.6$\,ms/tok, $0.961\pm0.001$\,tok/s).
INT4BW is significantly slower at $1{,}355.8\pm0.4$\,ms/tok
($0.737\pm0.001$\,tok/s), a \textbf{31\,\%} penalty relative to the other
4-bit formats despite an identical bit budget (see Discussion,
Section~\ref{sec:discussion}).
P-CTLE achieves $\mathbf{832.5\pm1.5}$\,ms/tok ($\mathbf{1.201\pm0.002}$\,tok/s),
a \textbf{19.6\,\%} improvement over CTLE, confirming that eliminating the
float multiply from the inner loop reduces per-token latency even on a RISC-V
core with hardware multipliers.

\paragraph{Prefill coherence.}
Prefill throughput (prefill\,ms\,$/$\,prompt\,tokens) matches generation
throughput within 2\,\% for all methods, confirming that the forward-pass
cost per token is stable regardless of position in the sequence and that
KV-cache growth over 64 tokens is negligible.

\paragraph{Memory.}
All 32\,MB of PSRAM are available at boot.
RunState (KV-cache $+$ RoPE tables $+$ activation struct) consumes 3{,}648\,KB
and is identical across formats.
Model weights occupy 7{,}551\,KB (INT4U/CTLE) or 9{,}435\,KB (INT4BW) in PSRAM;
the heap allocator adds a small alignment overhead relative to the on-disk size.
FP32 (60{,}777\,KB) exceeds the SPIFFS partition (13{,}312\,KB) and cannot
be deployed on-device.
Internal SRAM (624\,KB total) remains 570\,KB free throughout inference;
the 54\,KB used by the IDF runtime is constant across formats.

Figure~\ref{fig:mem_layout} illustrates this contrast: standard dequantization
expands a 4-bit model into a transient FP32 buffer that exceeds the PSRAM
allocation budget, while CTLE streams indices into CPU registers and leaves the
majority of PSRAM free for the KV-cache and application state.

% ── Figure: SRAM/PSRAM memory layout comparison ─────────────────────────────
\begin{figure*}[t]
\centering
\begin{tikzpicture}[
    scale=0.88, transform shape,
    font=\sffamily\small,
    memblock/.style={draw, rectangle, minimum width=3.6cm,
                     align=center, outer sep=0pt, inner sep=4pt},
    arrowstyle/.style={-Stealth, thick, gray!70!black, line width=1.1pt},
    flashmem/.style={draw, cylinder, shape border rotate=90, aspect=0.22,
                     fill=yellow!15, minimum height=1.1cm, minimum width=1.6cm,
                     align=center, font=\scriptsize}
]
% ── Y-axis (SRAM) ──
\def\limitY{6.5}
\def\colL{2.5}
\def\colR{11.0}
\draw[->, thick, gray] (-0.3,0) -- (-0.3,8.2) node[above, black, font=\small] {PSRAM usage};
\draw[dashed, red!70!black, thick] (-0.8,\limitY) -- (15.5,\limitY);
\node[text=red!70!black, font=\bfseries\scriptsize, anchor=south east]
    at (15.5,\limitY) {Available PSRAM ceiling (32\,MB)};
% ── A) Standard dequantization ──
\node[font=\bfseries] at (\colL,-0.9) {A) Standard dequantization};
\node[memblock, fill=gray!20, minimum height=1.3cm]  (base1) at (\colL,0.65) {IDF Runtime \\ (stacks, USB, log)};
\node[memblock, fill=blue!12, minimum height=1.5cm, anchor=south] (act1) at (base1.north) {RunState \\ (KV-cache, RoPE, activations)};
\node[memblock, pattern=north east lines, pattern color=red!45, draw=red!70!black,
      text=red!80!black, minimum height=4.5cm, anchor=south] (expand) at (act1.north) {};
\node[text=red!80!black, align=center, font=\bfseries\scriptsize]
    at (expand.center) {Dequantization buffer\\\textbf{(materialized FP32 weights)}};
\node[anchor=west, text=red!80!black, align=left, font=\bfseries\scriptsize]
    at ([xshift=0.15cm, yshift=1.2cm]expand.east)
    {$\leftarrow$ grows with model size};
\node[flashmem] (flash1) at (-2.8,4.2) {Flash \\ (4-bit)};
\draw[arrowstyle] (flash1) -- node[midway,above,sloped,font=\scriptsize]{read+expand} (expand.west);
% ── B) CTLE ──
\node[font=\bfseries] at (\colR,-0.9) {B) CTLE (ours)};
\node[memblock, fill=gray!20, minimum height=1.3cm]  (base2) at (\colR,0.65) {IDF Runtime \\ (stacks, USB, log)};
\node[memblock, fill=blue!12, minimum height=1.5cm, anchor=south] (act2) at (base2.north) {RunState \\ (KV-cache, RoPE, activations)};
\node[memblock, fill=techGreen!45, draw=techGreen!60!black,
      minimum height=0.55cm, anchor=south] (lut) at (act2.north) {Codebook LUTs ($\approx$64\,B/tensor)};
\node[circle, draw, thick, fill=white, inner sep=1pt, minimum size=0.9cm,
      drop shadow] (kernel) at (\colR,5.6) {\large$\sum$};
\node[left=0.08cm of kernel, font=\scriptsize, text=gray!80!black, align=right]
    {CPU\\regs};
\node[flashmem] (flash2) at (14.8,5.6) {Flash \\ (4-bit)};
\draw[arrowstyle] (flash2.west) -- node[midway,above,font=\scriptsize]{stream indices} (kernel.east);
\draw[dashed,->,thick,techGreen!60!black] (lut.north) -- (kernel.south);
\path (lut.north) -- (kernel.south) coordinate[midway] (midgap);
\node[text=techGreen!50!black, font=\bfseries\scriptsize, align=right, anchor=east]
    at ([xshift=-0.15cm]midgap)
    {Free PSRAM headroom\\\textbf{(21.5\,MB available)}};
\end{tikzpicture}
\caption{Memory layout comparison on ESP32-P4 Nano.
    \textbf{(A)} Standard dequantization materializes a full FP32 weight matrix
    in transient RAM before every matvec, growing proportionally with model size.
    \textbf{(B)} CTLE streams 4-bit indices directly from Flash into CPU registers
    and looks them up in a 64-byte per-tensor codebook, leaving 21.5\,MB of PSRAM
    free for the KV-cache, RoPE tables, and application state.}
\label{fig:mem_layout}
\end{figure*}

\paragraph{Generated text quality (CTLE-PSO, temp=0.8, top-$p$=0.9).}
All three prompts produce coherent TinyStories-style continuations:
\begin{itemize}
  \item \textit{``Once upon a time''} $\to$
        \textit{``\ldots there was a little girl named Lily. She loved to eat
        spaghetti because it was her favorite food\ldots''}
  \item \textit{``Tom and his dog went to the park''} $\to$
        \textit{``\ldots They were having a lot of fun playing with the ball.
        Suddenly, a big dog came running\ldots''}
  \item \textit{``The little girl named Lily''} $\to$
        \textit{``\ldots was very hungry. She had been playing all morning in the
        garden. Her favorite snack was a big, round orange ball\ldots''}
\end{itemize}

\paragraph{CPU note.}
The ESP32-P4 silicon revision~v1.3 operates at 360\,MHz (hardware cap;
SDK target 400\,MHz is adjusted by the bootloader).
Task-watchdog warnings during inference are expected and harmless: the
inference loop saturates CPU0, preventing IDLE0 from resetting the WDT,
but the system does not crash or reset.


% ─────────────────────────────────────────────────────────────────────────────
\subsection{P-CTLE: Product-LUT CTLE for throughput-oriented execution}
\label{sec:pctle}

P-CTLE extends CTLE by additionally quantising activation vectors to 4\,bits
at inference time, enabling a fully multiplier-free inner loop.
It should be interpreted as a compute-oriented variant rather than a drop-in
replacement for CTLE-PSO: CTLE-PSO provides the best memory--quality trade-off,
whereas P-CTLE targets higher throughput and hardware-native product lookups.
Given input activation $\mathbf{x}\in\mathbb{R}^N$ and a CTLE-compressed
weight matrix with codebook $\mathcal{L}_w\in\mathbb{R}^{16}$, P-CTLE:
(i)~quantises $\mathbf{x}$ to 16 uniform levels
$\mathcal{L}_a=\{x_{\min}+k\,\delta\}_{k=0}^{15}$,
$\delta=(x_{\max}-x_{\min})/15$, yielding $\mathbf{a}\in\{0,\ldots,15\}^N$;
(ii)~builds a 256-entry product LUT
$\mathcal{L}_p[a][w]=\mathcal{L}_a[a]\cdot\mathcal{L}_w[w]$ (256 multiplications,
amortised over $M{\times}N$ accumulations); and
(iii)~performs the inner loop without any multiplication:
\begin{equation}
y_m = \sum_{n=0}^{N-1} \mathcal{L}_p\!\left[a_n \,\|\, I_{m,n}\right],
\label{eq:pctle}
\end{equation}
where $a_n \,\|\, I_{m,n}$ is the 8-bit concatenated index.
The activation range changes with the input vector, so the product LUT is
rebuilt once per matvec call (per projected activation vector), not once per
row. The 256-entry LUT is 1\,KB in FP32 and can fit within a single
M9K-equivalent memory block on a Cyclone-IV-class FPGA.

\paragraph{Quality.}
P-CTLE (Table~\ref{tab:ablation}) achieves PPL\,=\,33.14 / 29{,}049
(TS / WT2), vs.\ 15.93 / 16{,}827 for CTLE-PSO—a $+$108\,\% / $+$73\,\%
increase due to the additional activation quantisation error.
Weight compression is unchanged (7.61\,MB, 7.98$\times$).
Thus, P-CTLE is not the quality-optimal configuration; it is the fastest
measured compressed-domain configuration.

\paragraph{Throughput.}
On the ESP32-P4 RISC-V HP@360\,MHz (Table~\ref{tab:hw}), P-CTLE achieves
$\mathbf{832.5\pm1.5}$\,ms/tok ($\mathbf{1.201\pm0.002}$\,tok/s), a
\textbf{19.6\,\%} speed gain over CTLE (1{,}035.9\,ms/tok).
Replacing \texttt{FMUL.S} $+$ \texttt{FADD.S} with an integer address
computation and a single \texttt{FADD.S} saves $\approx$4 RISC-V cycles
per inner-loop iteration; at 9.2\,M iterations for the embedding tensor
alone, the cumulative saving outweighs the per-matvec 256-multiplication
product-LUT rebuild overhead.

\paragraph{FPGA target.}
For Cyclone IV (or similar resource-constrained FPGAs), the P-CTLE
datapath requires zero DSP blocks in the inner loop: only integer bitwise
operations, BRAM reads (LUT\_p), and an integer accumulator.
P-CTLE demonstrates that CTLE-compressed tensors can be executed as
hardware-native product lookups, motivating a future CTLE-NPU coprocessor for
FPGA and RISC-V softcore platforms.

% ─────────────────────────────────────────────────────────────────────────────
\section{Discussion}
\label{sec:discussion}

\paragraph{INT4U vs.\ CTLE.}
Uniform INT4 with a single global scale fails catastrophically on transformers
whose weight distribution is multimodal and heavy-tailed.
This result---PPL rising from 9.35 to 11{,}070 on TinyStories (and
533{,}069 on WikiText-2)---is consistent with prior work on per-tensor INT4
baselines~\cite{frantar2023gptq,tomasev2019llm_quantization_survey},
and explains why per-group schemes (INT4BW) or learned codebooks (CTLE) are
necessary for sub-8-bit compression.

\paragraph{INT4BW vs.\ CTLE: quality--throughput trade-off.}
INT4BW achieves better PPL (10.71 vs.\ 15.93) by using finer-grained scale
adaptation, at the cost of 25\,\% higher storage (9.51 vs.\ 7.61\,MB).
The hardware benchmark reveals a second, qualitatively different cost:
INT4BW is \textbf{31\,\%} slower than CTLE on the same hardware
($1{,}356$ vs.\ $1{,}036$\,ms/tok), a gap that arises purely from PSRAM
bandwidth.
Each INT4BW matvec row requires reading
$\lceil N/G \rceil = \lceil 288/32 \rceil = 9$ per-group scale values from
the \texttt{scales[rows$\times$n\_groups]} table in PSRAM before processing
the nibble stream, effectively doubling PSRAM memory transactions per row.
INT4U eliminates this cost by using one scalar scale per tensor, and CTLE
replaces it with a 16-float LUT that is small enough to remain register- or
cache-resident throughout the row loop---neither format requires
additional PSRAM reads beyond the nibble stream itself.
As a result, INT4U and CTLE achieve statistically indistinguishable throughput
despite different dequantisation arithmetic (scalar multiply vs.\ LUT lookup).
The choice between INT4BW and CTLE+PSO therefore depends on the deployment
constraint: INT4BW is preferable when PPL is the primary metric and the
31\,\% throughput penalty is acceptable; CTLE+PSO is better when maximum
throughput and minimum footprint take priority.

\paragraph{Value of PSO over K-means.}
The $-$17.2\,\% PPL improvement of PSO over K-means on TinyStories
($-$12.5\,\% on WikiText-2) at zero additional memory cost demonstrates that
metaheuristic codebook optimization is a viable and practical technique for
TinyLM compression.
The gain is concentrated on the embedding matrix (9.2M weights, two orders of
magnitude larger than any other tensor), showing that metaheuristic search is
most valuable for large, high-impact tensors.
The fact that the improvement holds on both an in-distribution (TS) and an
out-of-distribution (WT2) corpus confirms that PSO finds a genuinely better
codebook, not one that merely overfits to a particular evaluation domain.
On small tensors ($\leq$82K weights), all three methods converge to nearly the
same quality, indicating that K-means is already near-optimal for those
projection matrices.

\paragraph{Throughput on ESP32-P4.}
The measured $0.965\pm0.001$\,tok/s for CTLE and $0.961\pm0.001$\,tok/s for
INT4U on the ESP32-P4 Nano RISC-V@360\,MHz are baselines without
RISC-V-specific kernel tuning.
Both INT4U and CTLE are dominated by PSRAM bandwidth---reading the nibble
stream---rather than arithmetic, explaining their near-identical throughput.
INT4BW, by contrast, adds a second PSRAM access pattern (scale reads) that
increases effective memory traffic per matvec row, reducing throughput by 31\,\%
independently of the nibble bandwidth.
The dominant compute cost per token is the output logit projection
(32\,000$\times$288 matvec), with the six attention and six FFN layers sharing
the remaining time.
RISC-V-specific optimisations (RVV vectorised nibble decode, unrolled LUT
scatter-accumulate) are expected to reduce the per-token time and are left for
future work.

\paragraph{Limitations.}
First, evaluation is on a single model family (TinyStories-15M); broader
testing across tasks and larger model families is needed.
Second, CTLE uses one codebook per matrix; group-wise codebooks or
mixed-precision extensions may improve quality.
Third, energy was not measured on the P4 (hardware instrumentation pending);
the 0.965\,tok/s result provides throughput only.


% ─────────────────────────────────────────────────────────────────────────────
\section{Conclusions and Future Work}
\label{sec:conclusions}

We presented CTLE, a 4-bit weight-sharing inference engine for IoT
microcontrollers that streams packed indices from Flash and performs
lookup-and-accumulate matrix--vector products without materializing dense
weight tensors in SRAM.
Three codebook learning strategies were compared: K-means, GA, and PSO.
PSO achieves the best quality ($-$17.2\,\% PPL vs.\ K-means on TinyStories;
$-$12.5\,\% on WikiText-2) at identical 7.98$\times$ compression, through
better optimization of the 9.2M-entry embedding tensor.
The consistent improvement across both an in-distribution corpus and the
standard WikiText-2 benchmark confirms that PSO finds a genuinely superior
codebook and not a domain-specific optimum.
INT4 Uniform collapses (PPL\,=\,11{,}070 / 533{,}069 on TS/WT2),
demonstrating that per-group or learned scales are necessary for 4-bit
transformers.
INT4 Block-wise achieves better PPL (10.71 / 7{,}120) at 25\,\% higher
footprint and with per-group scale overhead that carries a 31\,\% throughput
penalty on the RISC-V target.

On the ESP32-P4 Nano (RISC-V HP@360\,MHz), CTLE+PSO achieves 0.965\,tok/s,
loading a 7.61-MB model from SPIFFS in 2.69\,s and leaving 21.5\,MB of
PSRAM available for the KV-cache, RoPE tables, and application state.
Coherent TinyStories-style text is generated for all three benchmark prompts,
indicating that 4-bit CTLE+PSO preserves practical TinyStories-style
generation quality in this setting.

\paragraph{Future directions.}
(i) RISC-V-optimized CTLE kernel (RVV extension, unrolled nibble decoding).
(ii) Energy measurement on ESP32-P4 with hardware power meter.
(iii) CTLE-NPU: a streaming product-LUT micro-NPU on Cyclone IV FPGA
     with PicoRV32, reusing the GA/PSO codebook optimizer with hardware-aware
     fitness (INT8 fixed-point penalty)---target DATE/FPL/TCAS-II.
(iv) Group-wise codebooks or sparse residuals for improved quality at 4 bits.
(v) Broader evaluation across model families and downstream tasks.
(vi) Codebook learning guided by validation loss (task-aware optimization).


% ─────────────────────────────────────────────────────────────────────────────
\section*{Acknowledgments}
This work was funded by the Escuela de Ingeniería Informática,
Universidad de Valparaíso, Chile, through the grant REXE N.°\,2552/2023
(Programa de Doctorado en Ingeniería Informática Aplicada).
E.~Flores was supported by the ANID BECAS/DOCTORADO NACIONAL 21242003.


\bibliographystyle{IEEEtran}
\bibliography{references}


\section{Biography Section}

\begin{IEEEbiography}[{\includegraphics[width=1in,height=1.25in,clip,keepaspectratio]{photo.jpg}}]{Emilio Flores}
(Graduate Student Member, IEEE) received the B.S.\ degree in electrical
engineering from the Universidad de Tarapacá, Arica, Chile, in 2020, and the
Master's degree in artificial intelligence from the Universidad Internacional
de Valencia, Spain, in 2022.

He is currently pursuing the Ph.D.\ degree in applied computer engineering
with the Universidad de Valparaíso, Valparaíso, Chile.
He serves as Academic Secretary of Computing and Cybersecurity at
Universidad de las Américas, Santiago, Chile.
His research interests include embedded artificial intelligence,
metaheuristics for optimization, and computer vision.

Mr.\ Flores was a recipient of the ANID National Doctoral Scholarship in 2024.
\end{IEEEbiography}

\vspace{11pt}
\vfill

\end{document}
